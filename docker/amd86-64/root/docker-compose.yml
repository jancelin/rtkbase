version: '3'
services:
  web:
    image: jancelin/rtkbase:cmd
    ports:
      - 8000:8000
    volumes:
      - ./:/mnt/fs
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - ./cmd.env

